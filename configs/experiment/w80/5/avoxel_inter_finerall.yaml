# @package _global_

defaults:
  - w80/base_w80.yaml

datamodule:
  transforms:
    preparations:

      voxel: [0.5,0.5,0.1]
      y_definition: inter_finerall

model:
  num_classes: 9

  criterion:
    ignore_index: 8 # = num_classes_spt = num_classes_myria3d -1
# dataset_description:
#   num_classes: 9