# @package _global_

defaults:
  - w80/base_w80.yaml

datamodule:
  transforms:
    preparations:

      voxel: [0.5,0.5,0.1]
      y_definition: inter_finerbuilding

model:
  num_classes: 5
  
  criterion:
    ignore_index: 4 # num_classes

