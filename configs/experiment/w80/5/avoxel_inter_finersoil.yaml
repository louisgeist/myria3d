# @package _global_

defaults:
  - w80/base_w80.yaml

datamodule:
  transforms:
    preparations:

      voxel: [0.5,0.5,0.1]
      y_definition: inter_finersoil
  
  hdf5_file_path: "${hydra:runtime.cwd}/tests/data/flair3d_cp.hdf5" # PATH FOR NEW HDF5 PATH

model:
  num_classes: 6
  
  criterion:
    ignore_index: 5 # num_classes
